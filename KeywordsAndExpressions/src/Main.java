public class Main {

    public static void main(String[] args) {
        double kilometers = (100 * 1.609344); //statement -> data type + expression + ;
        // expression -> kilometer = (100 * 1.609344) -> returns a value.

        int highscore = 25;

        if (highscore > 25) { //highscore > 25 expression
            highscore = 1000 + highscore; // 2 expressions, 1000 + highscore and highscore = 1000 + highscore.
        }

    }
}
